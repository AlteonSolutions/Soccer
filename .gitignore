node_modules/
dist/
coverage/
.env
*.log
*.tsbuildinfo

# Claude Code
.claude/settings.local.json   # per-machine permissions; never shared
.claude/sessions.csv          # restored from the `worklog` branch by the SessionStart hook

# Customer data — blocked by format, sanitized fixtures whitelisted below by exception.
# Add a `!test/fixtures/<file>` line per fixture, never a blanket un-ignore.
*.xlsx
*.xlsm
*.csv
*.mdb
*.accdb
*.bak
!test/fixtures/*.csv
