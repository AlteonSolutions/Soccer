# Refuse to install on the wrong Node major instead of warning. A three-way disagreement about the
# Node version once produced a deploy that "succeeded" and then served nothing (see DECISIONS.md).
engine-strict=true
