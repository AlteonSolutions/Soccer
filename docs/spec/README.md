# docs/spec

What the app is supposed to do, feature by feature, before and while it is built. One file per
feature (`snack-signup.md`, `reminder-emails.md`), written to be read cold. When a spec describes
something not yet built, its first line says so.
