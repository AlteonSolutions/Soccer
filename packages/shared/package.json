{
  "name": "@soccer/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Types, zod schemas, the config module, the data guard and email — everything more than one app imports. Consumed as source: every app bundles it with esbuild.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas.ts",
    "./format": "./src/format.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@azure/communication-email": "^1.1.0",
    "@azure/data-tables": "^13.3.2",
    "@azure/storage-blob": "^12.33.0",
    "zod": "^4.5.4"
  }
}
